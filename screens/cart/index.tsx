import { SafeAreaView, Text } from 'react-native';

const CartSC: React.FC = () => {
    return (
        <SafeAreaView>
            <Text>Cart</Text>
        </SafeAreaView>
    );
};

export default CartSC;
