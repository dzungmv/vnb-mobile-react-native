import { SafeAreaView, Text } from 'react-native';

const PersonalSC: React.FC = () => {
    return (
        <SafeAreaView>
            <Text>Personal</Text>
        </SafeAreaView>
    );
};

export default PersonalSC;
