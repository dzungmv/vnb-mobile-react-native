import { SafeAreaView, Text } from 'react-native';

const NofiicationsSC: React.FC = () => {
    return (
        <SafeAreaView>
            <Text>Notifications</Text>
        </SafeAreaView>
    );
};

export default NofiicationsSC;
